from setuptools import setup, find_packages

__version__ = "0.0.1"

setup(
    name="section_identification",
    version=__version__,
    packages=["section_identification"],
    author="*********",
    author_email="*********",
    install_requires=[
        "numpy",
        "matplotlib"
    ]
)